FilterModal {
    align: center middle;
}

FilterModal > Vertical {
    width: 60;
    height: 14;
    border: thick $primary;
    background: $surface;
    padding: 1 2;
}

FilterModal .title {
    width: 100%;
    text-align: center;
    text-style: bold;
    margin-bottom: 1;
}

FilterModal .button-row {
    width: 100%;
    height: auto;
    align: center middle;
    margin-top: 1;
}

FilterModal .button-row Button {
    margin: 0 2;
}

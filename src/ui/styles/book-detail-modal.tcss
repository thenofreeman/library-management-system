BookDetailModal {
    align: center middle;
}

BookDetailModal > Vertical {
    width: 60;
    height: auto;
    border: thick $primary;
    background: $surface;
    padding: 1 2;
}

BookDetailModal #modal-title {
    width: 1fr;
    content-align: center middle;
}

BookDetailModal > Vertical > Button {
    width: 100%;
}

BookDetailModal .detail-line {
    margin: 1 0;
    height: auto;
}
